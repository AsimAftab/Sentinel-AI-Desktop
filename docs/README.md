# Historical documentation (v1 prototype)

Everything in this folder describes the **pre-rebuild prototype** (PyQt5
frontend + in-process backend, removed in PR #19) and is kept for reference
only. File paths, tool counts, and setup steps no longer match the codebase.

For current documentation see the repository root: `README.md`, `CLAUDE.md`,
`AGENTS.md`, `REBUILD_PLAN.md` (the plan that produced v2), and
`packaging/README.md`.
